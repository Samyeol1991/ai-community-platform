# AI 커뮤니티 플랫폼 개발 작업 요약

## 작업 일자
2024년 11월 25일

---

## 프로젝트 개요

AI 커뮤니티를 위한 게시판 시스템으로, Hugging Face 유해 콘텐츠 감지, 실시간 WebSocket 알림, 관리자/사용자 분리 인증을 특징으로 합니다. 관리자가 설정한 작성자 페르소나를 활용한 자동 댓글 생성 시스템과 3단계 댓글 품질 관리 시스템이 구축되어 있습니다.

---

## 1. 게시물 상세 화면 중복 내용 제거

### 문제점
게시물 상세 페이지에서 excerpt(요약)와 content(본문)가 함께 표시되어 내용이 중복으로 노출되는 문제 발생

### 해결 방법
- `client/src/pages/post.tsx` 파일 수정
- excerpt 표시 부분 제거하고 content만 표시하도록 변경

### 결과
게시물 내용이 한 번만 깔끔하게 표시됨

---

## 2. 작성자 성향 기반 자동 댓글 시스템

### 개요
관리자가 등록한 작성자 정보(이름 + 성향 설명)를 활용하여 GPT가 각 작성자의 성향에 맞는 댓글을 자동 생성하는 시스템

### 주요 기능

#### 2.1 동작 방식
1. 게시물 작성 후 30초 대기
2. GPT-4o-mini가 게시물 분석 (댓글 작성 가치 판단)
3. 댓글이 필요하다고 판단되면:
   - 관리자가 등록한 작성자 중 랜덤 선택
   - 선택된 작성자의 **성향 설명**을 GPT-5.1에 전달
   - GPT-5.1이 해당 성향에 맞는 어조와 스타일로 댓글 생성
   - 자동으로 댓글 등록

#### 2.2 작성자 관리
- **관리자 화면** > **AI 관리** > **작성자 정보 관리**에서 등록
- 각 작성자마다 이름과 성향 설명 입력 가능
- 예시:
  - 이름: "전문가 AI", 설명: "기술적인 내용을 깊이있게 분석하는 전문가"
  - 이름: "친절한 멘토", 설명: "초보자를 따뜻하게 격려하고 응원하는 멘토"
  - 이름: "감성터지는공대생", 설명: "I성향을 가지고 있어서 소심하지만, 남을 잘 배려하면서 공감하는 능력을 가지고 있어."

#### 2.3 봇 계정 생성
- 각 작성자마다 별도의 봇 계정 자동 생성
- 이메일 형식: `gpt-bot-{작성자ID}@ai-nexus.com`
- 작성자 정보가 없으면 기본 "AI 도우미" 사용

### 구현 파일
- `server/routes.ts`: 봇 계정 관리 및 자동 댓글 로직
- `server/openai-utils.ts`: GPT API 호출 및 성향 반영 로직
- `server/storage.ts`: 작성자 정보 CRUD
- `shared/schema.ts`: authorInfo 테이블 스키마

---

## 3. GPT 분석 버튼 제거

### 작업 내용
사용자가 수동으로 GPT 분석을 요청하는 버튼을 UI에서 완전히 제거

### 제거 위치
1. **게시물 상세 페이지** (`client/src/pages/post.tsx`)
   - "GPT 분석" 버튼 제거
   - 분석 결과 다이얼로그 제거
   - 관련 mutation 및 state 제거

2. **홈 화면 게시물 카드** (`client/src/pages/home.tsx`)
   - 각 게시물 카드의 "GPT 분석" 버튼 제거
   - 분석 결과 다이얼로그 제거

3. **댓글 섹션** (`client/src/components/comments-section.tsx`)
   - 댓글별 "GPT" 분석 버튼 제거
   - 분석 결과 다이얼로그 제거

### 비고
백엔드의 자동 댓글 생성 기능은 그대로 유지됨

---

## 4. 댓글 품질 관리 시스템 (3단계 프로세스)

### 개요
**목적에 따라 차별화된 AI 답글 시스템**

1. **게시물 작성 시**: 동기 부여를 위한 격려 댓글 자동 생성
2. **부적절한 댓글 작성 시**: 교육적 피드백으로 커뮤니티 문화 개선

### 4.1 3단계 프로세스

#### 1단계: Hugging Face 유해성 검사
- **모델**: `samyeol/kookmin_project` (Private HF Space)
- **기능**: 댓글의 유해성 점수(0~1) 산출
- **목적**: 객관적인 유해성 수치 제공
- **기존 활용**: 이미 댓글 작성 시 moderation 테이블에 저장됨

#### 2단계: GPT-4o-mini 답글 필요성 판단
- **모델**: GPT-4o-mini (temperature: 0.3)
- **입력**: 
  - 게시글 제목 및 내용
  - 댓글 내용 및 작성자
  - Hugging Face 유해성 점수
- **출력**: JSON 형식
  ```json
  {
    "needsReply": true/false,
    "purpose": "protect_author" | "improve_community" | "none",
    "reason": "판단 이유"
  }
  ```

**판단 기준**:
- **protect_author**: 게시글 작성자에 대한 부당한 공격/비난 → 작성자 보호 필요
- **improve_community**: 표현 방식이나 태도 개선 필요 → 커뮤니티 문화 개선
- **none**: 적절한 댓글 → 답글 불필요

#### 3단계: GPT-5.1 건설적 피드백 생성
- **모델**: GPT-5.1 (Responses API)
- **입력**:
  - 게시글 제목
  - 댓글 내용
  - 2단계에서 판단된 목적 (purpose)
  - 분석 이유 (reason)
  - 관리자 설정 작성자 성향 (persona)

**목적별 프롬프트 차별화**:

**A) protect_author (작성자 보호)**:
```
- 작성자에 대한 부당한 공격을 논리적으로 반박
- 댓글의 문제점을 객관적으로 지적
- 작성자를 옹호하되 감정적이지 않게
```

**B) improve_community (커뮤니티 개선)**:
```
- 댓글의 구체적인 문제점을 논리적으로 지적
- 왜 그러한 표현이 문제인지 설명
- 더 건설적이고 나은 표현 방법 제시
- 댓글 작성자가 개선할 수 있도록 유도
```

### 4.2 답글 특징

**작성자 보호형 답글**:
- 작성자에 대한 비난을 논리적으로 반박
- 공격의 문제점을 객관적으로 지적
- 감정적이지 않고 단호하게

**커뮤니티 개선형 답글**:
- 댓글의 문제점을 교육적으로 설명
- 더 나은 표현 방법을 구체적으로 제시
- 댓글 작성자가 스스로 개선하도록 유도
- 논리적이고 객관적인 어조 유지

### 4.3 기술 사양
- **분석 모델**: GPT-4o-mini (빠르고 정확한 판단)
- **생성 모델**: GPT-5.1 (고품질 답글 생성)
- **처리 시간**: 댓글 작성 후 2초
- **비동기 처리**: setTimeout으로 HTTP 응답 차단 없음
- **성향 반영**: 관리자가 등록한 작성자 페르소나 활용

### 4.4 로그 확인
서버 로그에서 3단계 프로세스 추적 가능:

```
[Comment Quality Check] Step 1: Hugging Face toxicity check for comment {id}...
[Comment Quality Check] Toxicity score: 45.2%

[Comment Quality Check] Step 2: Analyzing reply need with GPT-4o-mini...
[Comment Quality Check] Reply needed! Purpose: improve_community, Reason: ...

[Constructive Feedback] Step 3: Generating feedback with GPT-5.1 using bot: 감성터지는공대생
[Constructive Feedback] Feedback created successfully by 감성터지는공대생 (Purpose: improve_community)
```

### 구현 파일
- `server/openai-utils.ts`
  - `analyzeReplyNeed()`: 2단계 - GPT-4o-mini 판단
  - `generateConstructiveFeedback()`: 3단계 - GPT-5.1 생성
- `server/routes.ts`
  - 댓글 생성 API에 3단계 프로세스 통합
- `server/huggingface.ts`
  - `checkContentModeration()`: 1단계 - HF 유해성 검사

---

## 시스템 아키텍처

### 자동화 워크플로우

#### 게시물 작성 시 (동기 부여)
```
게시물 작성
  ↓
30초 대기
  ↓
GPT-4o-mini 분석 (댓글 가치 판단)
  ↓
[가치 있음] → 랜덤 작성자 선택 → GPT-5.1 격려 댓글 생성 (성향 반영) → 댓글 등록
[가치 없음] → 종료
```

#### 댓글 작성 시 (품질 관리)
```
댓글 작성
  ↓
1단계: Hugging Face 유해성 검사 (즉시, 기존 moderation 활용)
  ↓
2초 대기 (비동기, HTTP 응답 차단 없음)
  ↓
2단계: GPT-4o-mini 답글 필요성 판단
  - needsReply: true/false
  - purpose: protect_author / improve_community / none
  ↓
[답글 필요] → 랜덤 작성자 선택 → 3단계: GPT-5.1 목적별 피드백 생성 → 대댓글 등록
[답글 불필요] → 종료
```

### 주요 기술 스택
- **프론트엔드**: React, TypeScript, TailwindCSS, Wouter
- **백엔드**: Express.js, TypeScript, WebSocket
- **데이터베이스**: PostgreSQL, Drizzle ORM
- **AI 모델**: 
  - Hugging Face: `samyeol/kookmin_project` (유해성 감지)
  - GPT-4o-mini (빠른 분석 및 판단, 저비용)
  - GPT-5.1 (고품질 생성, Responses API)

---

## 관리자 설정 방법

### 작성자 정보 등록
1. 관리자 계정으로 로그인 (`admin/admin1234` 또는 `admin1/admin1234`)
2. 좌측 메뉴에서 **AI 관리** 클릭
3. **작성자 정보 관리** 섹션에서 작성자 추가
4. 각 작성자의 이름과 성향 설명 입력
   - 예: 이름 "감성터지는공대생", 설명 "I성향을 가지고 있어서 소심하지만, 남을 잘 배려하면서 공감하는 능력을 가지고 있어."

### 유해성 임계값 설정
1. 관리자 화면의 **유해 콘텐츠 관리** 섹션
2. 임계값 조정 (0~1 사이 값, 예: 0.7)
3. 임계값 이상의 유해성 점수를 받은 게시물/댓글은 자동 숨김 처리

### 테스트 방법

#### 자동 댓글 테스트
1. 일반 사용자로 게시물 작성
2. 30초 후 자동 댓글 확인
3. 작성자 성향이 반영되었는지 확인

#### 품질 관리 테스트
1. 부적절한 댓글 작성 (예: "이건 쓰레기네요")
2. 2초 후 서버 로그에서 3단계 프로세스 확인
3. 자동 생성된 교육적 피드백 답글 확인
4. 목적(protect_author 또는 improve_community)이 적절한지 확인

---

## 주요 개선 사항

### 사용자 경험
- 게시물 내용 중복 제거로 깔끔한 UI
- 불필요한 GPT 분석 버튼 제거로 단순화
- 3단계 품질 관리로 건강한 커뮤니티 유지
- 목적별 차별화된 피드백으로 효과적인 개선 유도

### 시스템 효율성
- 비동기 처리로 사용자 응답 차단 없음
- 3단계 파이프라인: HF → GPT-4o-mini → GPT-5.1
- 각 단계별 최적화된 모델 선택 (성능 vs 비용)
- 상세한 로깅으로 모니터링 및 디버깅 용이

### 확장성
- 관리자가 다양한 작성자 성향 등록 가능
- 각 작성자별 독립적인 봇 계정
- 목적별 프롬프트 커스터마이징 가능
- Hugging Face 유해성 데이터 재활용

### 보안 및 안정성
- Rate limiting 및 retry 로직 적용
- Error fallback 처리
- 환경 변수로 API 키 관리
- WebSocket 실시간 알림 시스템

---

## 데이터베이스 스키마

### 주요 테이블
- **users**: 사용자 및 봇 계정 정보
- **posts**: 게시물 (제목, 내용, 카테고리, 태그, 참여 지표)
- **comments**: 댓글 (parentCommentId로 대댓글 구조 지원)
- **author_info**: 작성자 정보 (id, name, description, createdAt)
- **content_moderation**: HF 유해성 검사 결과 (점수, 상태)
- **admin_thresholds**: 관리자 설정 임계값

---

## 향후 개선 제안

1. **통계 대시보드**
   - 목적별(protect_author vs improve_community) 답글 비율 분석
   - 작성자별 답글 효과 추적
   - 유해성 점수 분포 시각화

2. **AI 학습 데이터 축적**
   - 답글에 대한 사용자 반응 수집 (좋아요/싫어요)
   - 효과적인 피드백 사례 데이터베이스 구축
   - 프롬프트 개선을 위한 A/B 테스팅

3. **관리자 제어 강화**
   - 답글 생성 조건 세밀 조정 (유해성 점수 범위별)
   - 목적별 프롬프트 템플릿 편집 기능
   - 답글 생성 전 미리보기 및 승인 기능

4. **사용자 알림**
   - 건설적 피드백을 받았을 때 알림
   - 개선된 댓글 작성 시 긍정 피드백

---

## 개발자 정보

### 환경 변수
- `OPENAI_API_KEY`: OpenAI API 키 (GPT-4o-mini, GPT-5.1 사용)
- `HUGGINGFACE_TOKEN`: Hugging Face 토큰 (유해 콘텐츠 감지용)

### 관리자 계정
- 사용자명: `admin`, 비밀번호: `admin1234`
- 사용자명: `admin1`, 비밀번호: `admin1234`

### API 엔드포인트
- `POST /api/posts`: 게시물 작성 (30초 후 자동 댓글)
- `POST /api/comments`: 댓글 작성 (3단계 품질 검사)
- `GET /api/author-info`: 작성자 정보 목록
- `POST /api/author-info`: 작성자 정보 등록

---

## 문의사항

시스템 관련 문의사항이나 개선 요청은 개발팀에 문의해주세요.

---

## 변경 이력

### 2024-11-25
- 댓글 품질 관리 시스템을 3단계 프로세스로 개선
- 목적별(작성자 보호 vs 커뮤니티 개선) 답글 차별화
- Hugging Face 유해성 데이터를 GPT 판단에 통합
- 방어형 답글에서 교육형 피드백으로 전환

### 2024-11-24
- 게시물 상세 화면 중복 내용 제거
- 작성자 성향 기반 자동 댓글 시스템 구축
- GPT 분석 버튼 UI 제거
- 댓글 공격 감지 및 방어 답글 시스템 구축 (1단계)
